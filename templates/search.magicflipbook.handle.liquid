{% layout none %}{% capture results %}{% paginate search.results by 50 %}{% for item in search.results %}{ "id": {{ item.id | json }},"price": {{ item.price | json }},  "title": {{ item.title | json }}, "handle": {{ item.handle | json }},  "published_at": "{{ item.published_at | date: "%s" }}",{% if item.tags %}"tags": {{ item.tags | json }},"thumbnail":{{ item.featured_image.src | product_img_url: "800x" | json }},{% endif %}{% if item.compare_at_price %}"compare_at_price": {{ item.compare_at_price | json }},{% endif %}"variants": [{% for variant in item.variants %}{"id": {{ variant.id }}{% if variant.compare_at_price %},"compare_at_price": {{ variant.compare_at_price }}{% endif %}{% if variant.inventory_quantity %},"inventory_quantity": {{ variant.inventory_quantity }}{% endif %} }{% unless forloop.last %},{% endunless %}{% endfor %} ], "collections": [{% for collection in item.collections %}"{{ collection.id }}"{% unless forloop.last %},{% endunless %}{% endfor %}],"currency":{"name": "{{ cart.currency.name }}","code":"{{ cart.currency.iso_code  }}","symbol":"{{ cart.currency.symbol  }}"} } {% unless forloop.last %},{% endunless %}{% endfor %}{% endpaginate %}{% endcapture %}{"results_count":{{ search.results_count }}, "results":[{{ results }}] }