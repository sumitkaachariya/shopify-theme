<div class="sixteen columns clearfix collection_nav">
  <h1 class="collection_title"><h1 class="snize-search-results-page-title">{{ page.title }}</h1>

    {{ page.title }}
  </h1>
</div>

<div class="sixteen columns page">
    {{ page.content }}
</div>